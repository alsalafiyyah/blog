import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace with your actual domain or GitHub Pages URL
  site: 'https://alsalafiyyah.github.io',
  // Replace '<YOUR-REPOSITORY-NAME>' with your actual repo name (omit if it's a username.github.io root repo)
  base: '/blogmkdir -p .github/workflows',
});